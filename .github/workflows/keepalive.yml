name: Keep Render App Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping health endpoint
        run: |
          curl -s https://meeting-intelligence-system-c2tt.onrender.com/api/v1/health